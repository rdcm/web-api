[package]
name = "infra"
version = "0.0.1"
edition = "2021"

[dependencies]
async-trait = { version = "0.1.73", features = [] }
mongodb = "2.7.0"
domain = { path = "../domain" }
domain_impl = { path = "../domain_impl" }